验证下面的代码输出什么
  float v = 0;
  int a = 7;
  int b = 3;
  
  v = a / b;          printf: 2.000000
  v = a / (float) b;  printf: 2.333333  
  v = (float) a / b;  printf: 2.333333
  v = a / (float) b;
  
  printf("%f\n", v); 

代码里还包含着：表达式，类型精度的选择标准，根据除数还是被除数？根据精度最大的类型？

-------------------------------  
强制类型 操作符的 优先级
------------------------------- 
(类型) 强制类型转换 (数据类型)表达式 右到左结合(意思是多个强制类型转换符连用的情况)

强制类型转换 只小于 成员选择 和 负号运算符
  
  