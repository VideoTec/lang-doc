http://www.uml.org.cn/oobject/201012234.asp

对对象生命周期建模：主要描述对象
1）能够响应的事件、
2）对这些事件的响以及
3）过去对当前行为的影响

对反应型对象建模：
这个对象可能处于的稳定状态、
从一个状态到另一个状态之间的转换所需的触发事件，
以及每个状态改变时发生的动作 

状态机图既可以用来表示一个业务领域的知识，
也可以用来描述设计阶段对象的状态变迁