http://www.uml.org.cn/oobject/201012234.asp

延迟事件：
延迟事件是一种特殊的事件，
它是指该事件不会触发状态的转换，
当对象处于该状态时事件不会丢失，
但会被延迟执行。

例如，
当E-mail程序中正在发送第一封邮件时，
用户下达发送第二封邮件执令就会被延迟，
但第一封邮件发送完成后，这封邮件就会被发送。
这种事件就属于延迟事件