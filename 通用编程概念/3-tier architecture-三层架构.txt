引入问题原因：
http://developer.android.com/intl/zh-cn/reference/android/provider/ContactsContract.html
three-tier[英][θ'ri:t'aɪə] [美][θ'ri:t'aɪə]
[医]三层

ContactsContract defines 
an extensible database of contact-related information. 
Contact information is stored in a three-tier data model:

https://en.wikipedia.org/wiki/Multitier_architecture

In software engineering, 
multitier architecture (often referred to as n-tier architecture) 
is a client–server architecture in which presentation, application processing, 
and data management functions are physically separated. 
The most widespread use of multitier architecture is the three-tier architecture.

http://baike.baidu.com/link?url=ZT9NLbQ2p3ATOhCzNFXd_rlF1hoFNZLcdZ21xoNZFKqWUBQEf-EmVkQ4TA220ycePkMdLXly4D7dKBvaLG4CGa
三层架构(3-tier architecture) 
通常意义上的三层架构就是将整个业务应用划分为：
1）界面层（User Interface layer）、
2）业务逻辑层（Business Logic Layer）、
3）数据访问层（Data access layer）。

区分层次的目的即为了“高内聚低耦合”的思想。
在软件体系架构设计中，分层式结构是最常见，也是最重要的一种结构。

微软推荐的分层式结构一般分为三层，从下至上分别为：
1）数据访问层、
2）业务逻辑层（又或称为领域层）、
3）表示层。