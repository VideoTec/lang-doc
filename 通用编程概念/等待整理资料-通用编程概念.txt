开放封闭原则
软件实体（类、模块、函数）可以扩展，但是不可修改


工厂即是对构造函数的一种回调机制
http://cpp.ezbty.org/myfiles/boost/libs/functional/factory/doc/html/index.html
http://pastebin.com/dSTLt7vW 示例

三种工厂模式
简单工厂模式，传递参数告诉工厂对象，创建那个类的实例。

工厂方法模式，为每个类创建一个工厂类。调用不同的工厂类对象，来创建对应的类实例
抽象工厂模式，

明白了等级结构和产品族的概念，就理解工厂方法模式和抽象工厂模式的区别了，如果工厂的产品全部属于同一个等级结构，则属于工厂方法模式；如果工厂的产品来自多个等级结构，则属于抽象工厂模式


“工厂”是创建产品（对象）的地方，其目的是将产品的创建与产品的使用分离。抽象工厂模式的目的，是将若干抽象产品的接口与不同主题产品的具体实现分离开。这样就能在增加新的具体工厂的时候，不用修改引用抽象工厂的客户端代码


PLUG 组件是怎么加载所有对象的？
PLUG 组件是怎么加载所有对象的？
E:\代码\Plug2.8\PlugDev\Plug


幻灯片控件（视频播放进度条）
替代播放进度条，选择“时间区域”按钮


只要两个结构体的内存布局相同，它们就可以相互转换访问
为什么不直接让JNIEnvExt结构体从JNIEnv结构体继承下来呢？这样把一个JNIEnvExt结构体转换为一个JNIEnv结构体就是相当直观的。然而，Dalvik虚拟机的源代码并一定是要以C++语言的形式来编译的，它也可以以C语言的形式来编译的。由于C语言没有继承的概念，因此，为了使得Dalvik虚拟机的源代码能同时兼容C++和C，这里就使用了一个Trick：只要两个结构体的内存布局相同，它们就可以相互转换访问


建立一个自己的WEB服务http://trac.edgewall.org/
版本控制，BUG 


FIFO和LIFO是什么意思
FIFO: First in, First out.先进先出。

LIFO: Last in, First out.后进先出



uml 线程的描述
用GLSurfaceView的requesRender描述




doxygen


同步问题的产生，一般是，都可抽象为，对数据的处理问题
需要同步的原因，可抽象为，不同的进程与线程，访问同一个数据，时产生的问题。